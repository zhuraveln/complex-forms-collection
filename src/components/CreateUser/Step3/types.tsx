import * as yup from 'yup'

/** Types for useForm Step3 */
export interface IUserFieldsStep3 {
  files: FileList
}

/** Validation for fields useForm in Step3 */
// export const UserSchemaStep2 = yup.object().shape({
//   files: yup.
// })
